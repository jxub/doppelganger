-module(data_example).

-export([

]).

add(A, B) -> 
    A+B. 

is_val?(V) -> 
    (A=V+2A1=A*2A1). 

sum_list(L) -> 
    L|>List.foldl(0,FnEl,Acc->El+AccEnd). 

demo() -> 
    1. 

private(C) -> 
    {:ok,C}. 

